import { RAIL, SLEEPER } from '../../../utils/trackRenderingUtils';

export { RAIL, SLEEPER };

export const RAIL_COLORS = {
    DEFAULT: '#888888',
    SELECTED: '#00FF88',
    INACTIVE: '#555555',
};

export const NODE_KEY_COLORS = {
    DEFAULT: '#4ECDC4',
    CONNECT_SOURCE: '#00FF88',
    CONNECT_TARGET: '#00BFFF',
    INVALID: '#666666',
};

export const NODE_RADIUS = 6;
export const CONNECT_HIGHLIGHT_RADIUS = 12;

// Hit area width for easier clicking
export const HIT_STROKE_WIDTH = 16;
